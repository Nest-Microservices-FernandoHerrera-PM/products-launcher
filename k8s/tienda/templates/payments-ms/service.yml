apiVersion: v1
kind: Service
metadata:
  labels:
    app: payments-webhook
  name: payments-webhook
spec:
  ports:
    - name: http
      protocol: TCP
      port: 31489
      targetPort: 3000
  selector:
    app: payments-ms
  type: LoadBalancer
status:
  loadBalancer: {}
